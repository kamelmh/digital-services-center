# DSC — Digital Services Center
# Lock: 2026-08-20  (pip freeze from Python 3.14.6, Windows)
# Install:  pip install -r requirements.txt
# Offline:  all except --provider (groq/openrouter/aihubmix) need no API key

# ── Core ──────────────────────────────────────────────────
reportlab==4.5.1          # Arabic PDF (Tahoma + arabic_reshaper + bidi)
arabic-reshaper==3.0.1
python-bidi==0.6.11

# ── Web (Violit + API) ──────────────────────────────────
# violit==0.8.29           # Desktop UI framework ONLY (main.py/app_instance.py) — NOT used by the
#                          # Render API. It forces fastapi>=0.135 which conflicts with the API's pin.
#                          # For desktop builds: pip install violit==0.8.29 separately.
fastapi==0.110.3           # PINNED DOWN from 0.141.1 — 0.141's _IncludedRouter breaks real-server
                           # routing (all non-/ paths 404 behind gunicorn; fine in TestClient)
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0  # SaaS config (apps/api/app/core/config.py) — was missing, broke Render boot
requests==2.34.2
jinja2==3.1.6              # offline_templates + future prompts/*.j2

# ── Auth (P2 — SQLite + JWT, no extra infra) ────────────
PyJWT==2.13.0          # JWT (HS256)
# passlib/bcrypt kept as optional legacy (auth now uses hashlib pbkdf2)

# ── Data / Docs ─────────────────────────────────────────
openpyxl==3.1.5            # Excel import/export (future)
python-multipart==0.0.32   # FastAPI file uploads
email-validator==2.3.0     # Pydantic EmailStr

# ── Build ───────────────────────────────────────────────
# pyinstaller==6.22.2     # only for building dist/*.exe (pip install pyinstaller)

# ── Optional (local LLM lane, future) ───────────────────
# llama-cpp-python        # uncomment when adding bundled GGUF model
slowapi==0.1.9
